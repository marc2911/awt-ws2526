This is a very basic express app. If you run the express generator, you'll get exactly this.

To start with different port than `3000`, run

```bash
PORT=8000 npm start
```

(linux/mac) or

```cmd
set PORT=8000 && npm start
```

(windows).
