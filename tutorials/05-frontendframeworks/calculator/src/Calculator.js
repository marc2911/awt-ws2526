/**
 * @param {{ title: string }} props
 */
export default function Calculator({ title }) {
  return (
    <div>
      <h1>{title}</h1>
      <p>This is a simple calculator app.</p>
    </div>
  );
}
